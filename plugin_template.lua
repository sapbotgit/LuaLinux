local commands = { --Table/Dict type
}

local idle_cmd = { -- List type
}
local aliases = { -- Table/Dict type
}

return {
    commands = commands,
    idle_cmd = idle_cmd,
    aliases = aliases
}